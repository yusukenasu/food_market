<h1>商品情報編集</h1>

<%= form_with model: @product do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>

<%= button_to "削除", "/products/#{@product.id}", method: :delete %>
<%= link_to "戻る", 'javascript:history.back()' %>
