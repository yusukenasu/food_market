<% @items.first(5).each do |item| %>
  <div class="list-group">
   <div class="related_product_image"><%= image_tag (item['smallImageUrls'][0]) %><div>
   <%= link_to item.name, "#{item.url}" %>
   <%= number_with_delimiter(item.price) %>円
  </div>
<% end %>
  